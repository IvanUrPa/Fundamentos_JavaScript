/*Math*/
console.log(Math.random()); //regresa un valor aleatorio entre 0 y 1

let resultado;

resultado = Math.PI;
resultado = Math.round(2.6 ); //redondea el valor dependiendo de los decimales
resultado = Math.ceil(2.6 ); //redondea el valor hacia arriba
resultado = Math.floor(2.6 ); //redondea el valor hacia abajo
resultado = Math.sqrt(144); //calcular raiz cuadrada
resultado = Math.abs(-25); //convierte un numero negativo en positivo
resultado = Math.min(3,5,1,8,4); //indica cual es el valor minimo de un listado
resultado = Math.max(3,5,1,8,4); //indica cual es el valor maximo de un listado
resultado = Math.floor(Math.random() * 100); //indica cual es el valor minimo de un listado

console.log(resultado);
